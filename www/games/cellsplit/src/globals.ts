import { Vec2 } from './types';

/**
 * Camera pan [px]
 */
export const pan: Vec2 = { x: 0, y: 0 };
/**
 * World grid size [px]
 */
export const gridSize: Vec2 = { x: 10, y: 10 };
/**
 * Cell radius [px]
 */
export const cellSize = 5;
